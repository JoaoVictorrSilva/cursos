1º Instalação NodeJs - Ubuntu:
	- sudo apt update && sudo apt upgrade -y
	- curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
	- sudo apt install -y nodejs
	- node --version (conferir)

* node nome_arquivo.js -> para execução
* // -> comentário
* const -> declaração de variável